#include <stdio.h>
int main()
{
	int i=0;
	int n; 
	int sum=0;

	printf("Enter a number:");
	scanf("%d",&n);
	int numbers [n];
	while(i<n)
	{
		printf("enter numbers:");
		scanf("%d",&numbers[i]);
		sum=sum+numbers[i];
		++i;
	}
	{
		printf("the sum is:%d",sum);
	}


	return 0;
}
